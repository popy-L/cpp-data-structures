# 线性表元素交换

【问题描述】在【顺序表逆序】的程序基础上，增加一个算法，以第一个元素为分界线（基准），将所有小于等于它的元素移到该元素的前面，将所有大于它的元素移到该元素的后面。

【输入形式】线性表元素

【输出形式】移动操作之后的线性表

【样例输入】6 20 12 5 7 -1

【样例输出】5 6 12 20 7

【样例说明】

pivot=data[0]（基准）

j从后向前找小于等于pivot的元素：前移到data[i]处

i从前向后找大于pivot的元素：后移到data[j]处

重复前面的过程直到i>=j为止

